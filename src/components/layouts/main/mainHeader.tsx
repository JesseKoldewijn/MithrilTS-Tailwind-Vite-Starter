import m, { MComponent } from "@/utils/mithrilHelper";

const MainHeader: MComponent = () => {
  return {
    controller: () => {},
    view() {
      return <>Header</>;
    },
  };
};
export default MainHeader;
